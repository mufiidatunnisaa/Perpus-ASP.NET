@model IEnumerable<Final.Models.tabel_peminjam>

@{
    ViewBag.Title = "Index";
}

<div class="row">
    <div class="box">
        <div class="col-lg-12">
            <h2>Index</h2>

            <p>
                @Html.ActionLink("Create New", "Create")
            </p>
            <table class="table">
                <tr>
                    <th>
                        @Html.DisplayNameFor(model => model.nama_peminjam)
                    </th>
                    <th>
                        @Html.DisplayNameFor(model => model.alamat_peminjam)
                    </th>
                    <th>
                        @Html.DisplayNameFor(model => model.password_peminjam)
                    </th>
                    <th></th>
                </tr>

                @foreach (var item in Model)
                {
                    <tr>
                        <td>
                            @Html.DisplayFor(modelItem => item.nama_peminjam)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.alamat_peminjam)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.password_peminjam)
                        </td>
                        <td>
                            @Html.ActionLink("Edit", "Edit", new { id = item.id_peminjam }) |
                            @Html.ActionLink("Details", "Details", new { id = item.id_peminjam }) |
                            @Html.ActionLink("Delete", "Delete", new { id = item.id_peminjam })
                        </td>
                    </tr>
                }

            </table>
        </div>
    </div>
</div>
