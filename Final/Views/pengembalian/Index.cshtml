@model IEnumerable<Final.Models.tabel_pengembalian>

@{
    ViewBag.Title = "Index";
}

<div class="row">
    <div class="box">
        <div class="col-lg-12">
            <h2>Index</h2>

            <p>
                @Html.ActionLink("Create New", "Create")
            </p>
            <table class="table">
                <tr>
                    <th>
                        @Html.DisplayNameFor(model => model.tanggal_kembali)
                    </th>
                    <th>
                        @Html.DisplayNameFor(model => model.denda)
                    </th>
                    <th>
                        @Html.DisplayNameFor(model => model.tabel_peminjaman.id_peminjaman)
                    </th>
                    <th></th>
                </tr>

                @foreach (var item in Model)
                {
                    <tr>
                        <td>
                            @Html.DisplayFor(modelItem => item.tanggal_kembali)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.denda)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.tabel_peminjaman.id_peminjaman)
                        </td>
                        <td>
                            @Html.ActionLink("Edit", "Edit", new { id = item.id_pengembalian }) |
                            @Html.ActionLink("Details", "Details", new { id = item.id_pengembalian }) |
                            @Html.ActionLink("Delete", "Delete", new { id = item.id_pengembalian })
                        </td>
                    </tr>
                }

            </table>
        </div>
    </div>
</div>