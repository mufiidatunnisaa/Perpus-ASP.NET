@model IEnumerable<Final.Models.tabel_pegawai>

@{
    ViewBag.Title = "Index";
}

<div class="row">
    <div class="box">
        <div class="col-lg-12">
            <h2>Index</h2>

            <p>
                @Html.ActionLink("Create New", "Create")
            </p>
            <table class="table">
                <tr>
                    <th>
                        @Html.DisplayNameFor(model => model.nama_pegawai)
                    </th>
                    <th>
                        @Html.DisplayNameFor(model => model.password_pegawai)
                    </th>
                    <th></th>
                </tr>

                @foreach (var item in Model)
                {
                    <tr>
                        <td>
                            @Html.DisplayFor(modelItem => item.nama_pegawai)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.password_pegawai)
                        </td>
                        <td>
                            @Html.ActionLink("Edit", "Edit", new { id = item.id_pegawai }) |
                            @Html.ActionLink("Details", "Details", new { id = item.id_pegawai }) |
                            @Html.ActionLink("Delete", "Delete", new { id = item.id_pegawai })
                        </td>
                    </tr>
                }

            </table>
        </div>
    </div>
</div>